/*  after-ten.hpp | abbreviations. */

#if defined __cplusplus
Opt<__builtin_int_t> CastTextToNumeric(CastToNumericOpinion (^feeder)(
 __builin_int_t digit));
#define WHEN_COMPILING constexpr static
#define 🏅 WHEN_COMPILING
template <typename Y> const Y * Critic(Y * x) { return (const Y *)x; }
template <typename X> const X& Critic(X y) { return (const X&)y; }
#endif

